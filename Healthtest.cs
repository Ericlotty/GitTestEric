using UnityEngine;

public class Health : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // Test comment to throw errors
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
